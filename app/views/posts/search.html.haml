= render "shared/header"


.search-main
  .search-top
    =form_tag('/posts/search', method: :get, placeholder:"") do
      -# .search__box
      = label_tag "SEARCH"
      = text_field_tag :keyword, :'', placeholder: " 検索したい国や気になるキーワードを入力", class: "search-input"
    %p ※検索前は最新投稿が表示されます！
  .search-bottom
    .search-result
      -@posts.each do |p|
        .search-result__each     
          = link_to post_path(p.id), alt: "#{p.title}<br/>#{p.country}" do
            = image_tag(p.image.url)

    = paginate @posts

= render "shared/footer"
